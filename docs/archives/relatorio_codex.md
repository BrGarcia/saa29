# Relatorio Codex

Escopo analisado: `app/`, `templates/`, `static/`, `docs/`, `tests/`

Validacao: `pytest tests -q` em `2026-04-14` -> `70 passed`

| Issue | Severity | Unused / Redundant | Location | Fix |
|---|---|---|---|---|
| Logout nao invalida sessao no fluxo web e a blacklist nao escala com Gunicorn. O botao "Sair" so apaga o token local; quando `/auth/logout` e chamado manualmente, a revogacao fica presa ao processo atual. | critical | `/auth/logout` esta orfao no frontend; blacklist em memoria perde efeito com multiplos workers. | `templates/base.html:67`, `static/js/app.js:49-52`, `app/auth/router.py:57-76`, `app/auth/security.py:19-21,92-103`, `gunicorn_conf.py:6-8` | Fazer `clearAuth()` chamar `POST /auth/logout` antes de limpar storage e mover a blacklist para Redis/DB compartilhado, ou trocar para sessao server-side/refresh token com revogacao real. |
| `DATABASE_URL` para PostgreSQL e sobrescrita silenciosamente para SQLite quando `APP_ENV=development`. Com `docker-compose`, isso pode mandar a aplicacao para um `.db` efemero no container e mascarar bugs especificos de PostgreSQL. | high | O validador `validate_db_url()` substitui a configuracao informada pelo operador. | `app/config.py:47-53`, `.env.example:10-16`, `docker-compose.yml:1-35` | Remover o rewrite implicito; usar flag explicita para SQLite local ou falhar no startup quando `DATABASE_URL` nao for a esperada para o ambiente. |
| Falha no upload de evidencia nao interrompe o fluxo de criacao de pane. A pane e criada e o usuario recebe sucesso mesmo se o anexo falhar por validacao, auth ou rede. | high | O upload ignora `response.ok` e duplica fluxo HTTP fora de `apiFetch()`. | `templates/panes/lista.html:387-406` | Usar `apiFetch()` ou checar o status do `fetch`; se o upload falhar, exibir erro e permitir retry. Melhor: endpoint backend transacional para criar pane + anexo no mesmo fluxo. |
| A autenticacao do frontend depende de `localStorage` para token, perfil e gate de tela. Isso expoe material sensivel ao browser e deixa as paginas HTML "protegidas" apenas por redirect client-side. | medium | Estado duplicado no cliente: `saa29_access_token` + `saa29_user`. | `static/js/app.js:39-50`, `templates/login.html:79-83`, `templates/base.html:83-99`, `app/pages/router.py:22-55` | Migrar para cookie `HttpOnly`/`SameSite` e aplicar autenticacao no backend das rotas HTML; manter `localStorage` apenas para preferencias nao sensiveis. |
| Documentacao principal esta desatualizada e contradiz o estado real do projeto. Ainda descreve fase de stubs, usa `INSPETOR`, aponta links quebrados e documenta uma arquitetura que nao existe na implementacao atual. | medium | Conteudo legado do "Dia 3/Dia 4", exemplos obsoletos e links mortos. | `docs/CONTRIBUTING.md:10-15,177-198`, `docs/api/referencia-api.md:20-31`, `docs/README.md:59-61`, `docs/architecture/overview.md:23-29,51-54` | Reescrever docs para a versao atual, mover material historico para `docs/archives`, corrigir links e alinhar exemplos/arquitetura com o codigo em producao. |
| Ha codigo morto ou nao exercitado que aumenta custo de manutencao sem entregar funcionalidade. | low | `LoginRequest` nunca e usado; `IntegrityError` e import morto; `remover_aeronave()` e `atualizar_equipamento()` nao tem rota/consumo; `editPane()` e o parametro `matricula` sao mortos no frontend. | `app/auth/schemas.py:18-21`, `app/auth/service.py:11`, `app/aeronaves/service.py:151-198`, `app/equipamentos/service.py:57-69`, `templates/panes/lista.html:283,435-439` | Remover o que nao sera usado ou expor/testar formalmente o que deveria virar funcionalidade. |
