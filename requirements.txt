# ============================================================
# SAA29 – Sistema de Gestão de Panes – Eletrônica A-29
# requirements.txt
# ============================================================

# --- Framework web ---
fastapi==0.115.6
uvicorn[standard]==0.32.1

# --- ORM e Banco de Dados ---
sqlalchemy==2.0.36
alembic==1.14.0
asyncpg==0.30.0          # driver async para PostgreSQL
psycopg2-binary==2.9.10  # driver sync (fallback / scripts)

# --- Validação e Settings ---
pydantic==2.10.4
pydantic-settings==2.7.0

# --- Autenticação ---
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
python-multipart==0.0.20  # form data (login)

# --- Upload de arquivos ---
python-magic==0.4.27      # validação de MIME type
jinja2==3.1.5             # templates frontend

# --- Utilitários ---
python-dotenv==1.0.1
python-dateutil>=2.9.0     # relativedelta para cálculo de vencimentos

# --- Testes ---
pytest==8.3.4
pytest-asyncio==0.24.0
httpx==0.28.1             # TestClient assíncrono
anyio==4.7.0
aiosqlite>=0.21.0         # Driver SQLite async para testes in-memory

boto3
python-dotenv
