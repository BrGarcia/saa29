# ============================================================
# SAA29 – Sistema de Gestão de Panes – Eletrônica A-29
# requirements.txt
# ============================================================

# --- Framework web ---
fastapi==0.115.6
uvicorn[standard]==0.32.1
gunicorn==23.0.0

# --- ORM e Banco de Dados ---
sqlalchemy==2.0.36
alembic==1.14.0
aiosqlite>=0.21.0         # Driver SQLite async

# --- Validação e Settings ---
pydantic==2.10.4
pydantic-settings==2.7.0

# --- Autenticação ---
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
bcrypt==3.2.0             # Versão específica para evitar erro com passlib
python-multipart==0.0.20  # form data (login)

# --- Upload de arquivos ---
python-magic==0.4.27      # validação de MIME type
jinja2==3.1.5             # templates frontend

# --- Módulo Publicações ---
# Extração de texto PDF página a página, para o índice de busca dos manuais.
# Escolhido em vez do PyMuPDF por licença: pypdfium2 é Apache-2.0/BSD-3, e o
# PyMuPDF é AGPL — inviável aqui (ADR-004). Só é usado offline, pelo indexador.
pypdfium2==5.12.1

# --- Utilitários ---
python-dotenv==1.0.1
python-dateutil>=2.9.0     # relativedelta para cálculo de vencimentos

# --- Testes ---
pytest==8.3.4
pytest-asyncio==0.24.0
httpx==0.28.1             # TestClient assíncrono
anyio==4.7.0
aiosqlite>=0.21.0         # Driver SQLite async para testes in-memory

boto3
python-dotenv
fastapi-csrf-protect==0.3.3
slowapi
Pillow==11.0.0
pillow-heif==0.21.0
imgdiet==1.0.6
openpyxl>=3.1.0
reportlab>=4.0.0
